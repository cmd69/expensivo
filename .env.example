# ============================================================================
# Archivo de ejemplo de variables de entorno para Expensivo
# ============================================================================
# Copia este archivo a .env y configura los valores según tu entorno:
#   cp .env.example .env
#   nano .env
# ============================================================================

# ============================================================================
# PostgreSQL - Configuración de la base de datos
# ============================================================================
POSTGRES_USER=expensivo_user
POSTGRES_PASSWORD=admin
POSTGRES_DB=expensivo_db
POSTGRES_PORT=5432

# ============================================================================
# Backend - Configuración de la aplicación
# ============================================================================
# Nombre de la aplicación
APP_NAME=Expensivo

# Entorno: production, development, staging
APP_ENV=production

# Modo debug (true/false) - Solo habilitar en desarrollo
DEBUG=false

# Clave secreta para JWT y encriptación (OBLIGATORIO - generar una clave segura)
# Puedes generar una con: openssl rand -hex 32
SECRET_KEY=change-this-secret-key-in-production-generate-with-openssl-rand-hex-32

# Algoritmo para JWT
ALGORITHM=HS256

# Tiempo de expiración del token de acceso (en minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ============================================================================
# CORS - Orígenes permitidos
# ============================================================================
# URLs separadas por comas que pueden acceder al backend
# Ejemplo: http://localhost:3030,http://192.168.1.72:3030,https://tudominio.com
CORS_ORIGINS=http://localhost:3030,http://localhost:3000

# ============================================================================
# Admin User - Usuario administrador inicial
# ============================================================================
# Credenciales del usuario administrador que se creará al iniciar la aplicación
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin

# ID de chat de Telegram para notificaciones (opcional)
ADMIN_TELEGRAM_CHAT_ID=

# ============================================================================
# Redis - Configuración del cache (opcional)
# ============================================================================
# URL de conexión a Redis (por defecto usa el servicio redis del docker-compose)
REDIS_URL=redis://redis:6379/0

# ============================================================================
# Puertos - Puertos expuestos en el host
# ============================================================================
# Puerto del backend (mapeado al puerto 8000 del contenedor)
BACKEND_PORT=8008

# Puerto del frontend (mapeado al puerto 3030 del contenedor)
FRONTEND_PORT=3030

# Puerto de Redis (mapeado al puerto 6379 del contenedor)
REDIS_PORT=6379

# ============================================================================
# Frontend - Configuración del frontend
# ============================================================================
# URL del backend para el frontend (opcional - auto-detecta si está vacío)
# Solo configurar si necesitas override (ejemplo: http://tu-servidor:8008)
NEXT_PUBLIC_API_URL=

# Entorno de Node.js
NODE_ENV=production
